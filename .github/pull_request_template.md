## What

<!-- What does this PR change, in one or two sentences? -->

## Why

<!-- What problem does it solve? Link issues with "Closes #N". -->

## Checks

- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] UI changes follow the design tokens (no arbitrary sizes/colors) and were checked in both `pnpm tauri dev` and browser mode
