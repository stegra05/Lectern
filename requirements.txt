# Core dependencies for Lectern CLI

# PDF parsing and image extraction (lightweight alternative to PyMuPDF)
# NOTE: Requires Poppler system dependency (`brew install poppler` on macOS)
pypdf>=4.0.0
pdf2image>=1.16.0
pytesseract>=0.3.10

# Google Gemini API client
google-genai>=1.0.0

# HTTP requests for AnkiConnect communication
requests>=2.31.0,<2.33

# Web Server & API
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# GUI
pywebview>=5.0
pyobjc-framework-Cocoa>=10.0

# CLI enhancements
rich>=13.7.1,<14.0

# Security
keyring>=24.0.0

# Testing (dev only, excluded from bundle)
pytest>=8.0.0