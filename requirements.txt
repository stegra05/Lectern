# Core dependencies for Lectern

# Google Gemini API client
google-genai>=1.51.0

# PDF metadata extraction (text, images, page count)
pypdf>=4.0.0

# HTTP requests for AnkiConnect communication
httpx>=0.27.0

# Web Server & API
# NOTE: FastAPI + Pydantic are pinned to exact versions so the OpenAPI schema
# (and the generated TypeScript client) is byte-identical across environments.
fastapi==0.136.0
pydantic==2.12.5
uvicorn>=0.27.0
python-multipart>=0.0.9

# GUI
# Pin pywebview to reduce Windows packaging/runtime drift across environments.
pywebview==6.1
pyobjc-framework-Cocoa>=10.0; platform_system=="Darwin"
# NOTE(Windows): pythonnet 3.0.3+ has better clr_loader support for PyInstaller's
# _internal/ layout. Ensures Python.Runtime.dll is resolvable as a safety net
# even if pywebview falls back to the WinForms/mshtml backend.
pythonnet==3.0.3; platform_system=="Windows"

# Environment file support (optional)
python-dotenv>=1.0.1

# Security
keyring>=24.0.0

# Testing (dev only, excluded from bundle)
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Caching utilities
cachetools>=5.0.0
